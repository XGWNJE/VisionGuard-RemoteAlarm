# VisionGuard Server 环境变量配置
# 复制此文件为 .env 并修改

# 监听端口
PORT=3000

# 共享 API Key (所有端使用同一个，必须设置)
API_KEY=your-secret-key-here

# 截图过期时间 (小时)，默认 72
SCREENSHOT_TTL_HOURS=72

# 截图清理间隔 (毫秒)，默认 3600000 (1小时)
# CLEANUP_INTERVAL_MS=3600000

# 上传大小限制 (字节)，默认 2097152 (2MB)
# MAX_UPLOAD_BYTES=2097152
